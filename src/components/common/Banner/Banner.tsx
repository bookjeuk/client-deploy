import React from 'react'

const Banner = () => {
  const styles = {
    bannerContainer: "w-full h-96 bg-light-primary flex justify-center items-center"
  }
  return (
    <section className={styles.bannerContainer}>
      Banner
    </section>
  )
}

export default Banner